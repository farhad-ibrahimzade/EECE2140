# Get a list of numbers and find if there is a contigous sequence that has a sum 0

lst = [int(num) for num in input("Enter the list of numbers").split()] # list of numbers

sums = []

for nums in lst:
    sum += nums
    sums.append(sum)

for s in sums:
    